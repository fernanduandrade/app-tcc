export const initialState = {
  favorites: [],
}

export const UserReducer = (state, action) => {
  switch (action.type) {
    case 'setFavorite':
      // setar os lugares favoritos
      break;
    default:
      return state
  }
}